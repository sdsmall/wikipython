application: wikipythontest
version: 1
runtime: python27
api_version: 1
threadsafe: false

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: code.app

libraries:
- name: ssl
  version: latest